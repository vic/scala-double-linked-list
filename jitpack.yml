before_install:
  - curl -o scala-cli https://raw.githubusercontent.com/VirtusLab/scala-cli/main/scala-cli.sh
  - chmod +x scala-cli
install:
  - ./scala-cli --power publish --server=false --publish-repository $HOME/.m2/repository .
